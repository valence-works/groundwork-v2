{
  "Groundwork": {
    "Provider": "sqlite",
    "ConnectionString": "Data Source=groundwork-sample.db",
    "//": "Development only. Stands the local database up so `dotnet run` works without a deployment step. Production physical schema belongs to `groundwork apply`.",
    "DevelopmentApplySchema": true
  }
}
