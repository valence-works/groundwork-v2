# Groundwork v2 0.4.0-preview.18

Published release notes for #424 and #425. Package publication and exact-feed
verification ran in canonical release run `34163395163` for merge
`f03340bf5394399a7dba1f783baea598f7a2b2bb`; the full provider CI gate was green
before publication.

This preview narrows the SQLite provider gate so that file-backed sessions are no
longer queued behind an unrelated caller's unit of work, and inserts a MongoDB
exact-append payload in one round trip instead of three per document.

## SQLite provider gate scope

`0.4.0-preview.9` serialized every SQLite session open and every non-unit-of-work
session command on one provider gate and held that gate for the complete
lifetime of a unit of work. A host that commits background batches in units of
work on the same provider connection its request path uses therefore paid the
whole batch lifetime on every request-path session open and command; Elsa
Foundation measured its HTTP p95 doubling on adoption
(valence-works/groundwork-v2#424).

The gate now covers only what shares state: units of work against each other,
schema and catalog work, provider disposal, and commands on a shared in-memory
connection. A session on a file-backed store opens and runs on its own
connection while another caller's unit of work is active. Its reads proceed
under WAL; its writes wait on SQLite's own writer lock through the connection's
busy timeout instead of on the provider gate. Concurrent callers sharing one
session view are serialized on a per-session gate, as the PostgreSQL and SQL
Server views already were. The transferable unit-of-work lease, its terminal
release, the observer re-entry refusals, and deferred provider cleanup while a
unit of work is open are unchanged. A unit of work can no longer be begun from
inside a command observer callback on any thread; that refusal previously
applied only when the callback ran on the gate-holding thread.

Provider disposal now takes a registry lock around session registration, so a
session that is registering while `Dispose` starts is still closed by that
disposal rather than missed.

## MongoDB exact-append payload shape

`MongoStorageSession.AppendCore` inserted an exact-append payload one document
at a time: a counter `FindOneAndUpdate` for the provider sequence, an
`InsertOne`, and a high-water update per row, so a 500-row append inside its
transaction cost about 1,500 sequential round trips. Elsa Foundation's
diagnostics drain measured 25 to 45 records per second on a hosted replica set
(valence-works/groundwork-v2#425).

An exact append now allocates its whole sequence range with one counter
command, encodes the documents locally, inserts them with one ordered
`InsertMany` inside the same transaction, and records the high water once. The
observer sees one `mongodb.provider-sequence` and one
`mongodb.append-insert-many` event per payload. A duplicate key or normalized
transient write conflict still rejects the whole payload and rolls the ledger
back with it. Units with optimistic concurrency keep the per-document path,
because their version metadata is written per row. The provider-sequence
capability text and the generated provider matrix describe the new cost;
ordinary single-row generated writes still use one counter command each.

## Upgrade gate

The `0.4.0-preview.18` package closure is published and exact-feed restore is
verified by the canonical release run above. Adopt the release only from that
exact Feedz closure; local proof packages are development artifacts, not the
final consumer dependency.
